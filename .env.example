# Database Configuration (Optional - sử dụng in-memory storage nếu không có)
# DATABASE_URL=postgresql://username:password@localhost:5432/portfolio_db

# Object Storage Configuration (Optional - chỉ cần thiết nếu dùng cloud storage)
# DEFAULT_OBJECT_STORAGE_BUCKET_ID=your_bucket_id
# PRIVATE_OBJECT_DIR=/your_bucket/private
# PUBLIC_OBJECT_SEARCH_PATHS=/your_bucket/public

# Session Configuration (Optional - có default value)
# SESSION_SECRET=your_super_secret_session_key_here

# Development Configuration
NODE_ENV=development
PORT=5000

# Admin Configuration 
ADMIN_PASSWORD=admin2025